<h1>シフト申請一覧</h1>

  <table class="table">
    <tr>
      <th>出勤日</th>
      <th>出勤時間</th>
      <th>退勤時間</th>
      <th>詳細</th>
      <th>申請状態</th>
      <th>修正</th>
      <th>削除</th>
    </tr>
    <% @attendance_requests.each do |attendance| %>
    <tr>
      <td><%= attendance.schedule.starting_time %></td>
      <td><%= attendance.schedule.starting_time %></td>
      <td><%= attendance.schedule.closing_time %></td>
      <td><%= link_to '詳細', attendance_request_path(attendance_request.id) %></td>
      <td></td>
      <td><%= link_to "編集", edit_attendance_request_path(attendance_request.id), data: { confirm: '本当に編集していいですか？' } %></td>
      <td><%= link_to '削除', attendance_request_path(attendance_request.id), method: :delete, data: { confirm: '本当に削除していいですか？' } %></td>
    </tr>
    <% end %>
  </table>

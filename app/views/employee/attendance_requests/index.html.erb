<h1>シフト申請一覧</h1>

  <table class="table">
    <tr>
      <th>出勤日</th>
      <th>出勤時間</th>
      <th>退勤時間</th>
      <th>詳細</th>
      <th>申請状態</th>
    </tr>
    <% @attendance_requests.each do |attendance| %>
    <tr>
      <td><%= attendance.schedule.starting_time.strftime('%Y/%m/%d') %></td>
      <td><%= attendance.schedule.starting_time.strftime('%H:%M') %></td>
      <td><%= attendance.schedule.closing_time.strftime('%H:%M') %></td>
      <td><%= link_to '詳細', employee_attendance_request_path(attendance) %></td>
    </tr>
    <% end %>
  </table>
